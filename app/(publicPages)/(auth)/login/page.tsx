import { LoginCard } from "@/components/featureSpecific/login-card";

const page = () => {
  return (
    <section className="w-full min-h-[80vh] flex items-start justify-center pt-10 md:pt-20">
      <LoginCard />
    </section>
  );
};

export default page;
